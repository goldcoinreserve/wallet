"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CreateTransactionFromDTO = exports.extractMosaics = exports.extractRecipient = void 0;
/*
 * Copyright 2019 NEM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const format_1 = require("../../core/format");
const utils_1 = require("../../core/utils");
const model_1 = require("../../model");
const account_1 = require("../../model/account");
const mosaic_1 = require("../../model/mosaic");
const namespace_1 = require("../../model/namespace");
const transaction_1 = require("../../model/transaction");
/**
 * Extract recipientAddress value from encoded hexadecimal notation.
 *
 * If bit 0 of byte 0 is not set (e.g. 0x90), then it is a regular address.
 * Else (e.g. 0x91) it represents a namespace id which starts at byte 1.
 *
 * @param recipientAddress {string} Encoded hexadecimal recipientAddress notation
 * @return {Address | NamespaceId}
 */
exports.extractRecipient = (recipientAddress) => {
    if (typeof recipientAddress === 'string') {
        return utils_1.UnresolvedMapping.toUnresolvedAddress(recipientAddress);
    }
    else if (typeof recipientAddress === 'object') {
        // Is JSON object
        if (recipientAddress.hasOwnProperty('address')) {
            return account_1.Address.createFromRawAddress(recipientAddress.address);
        }
        else if (recipientAddress.hasOwnProperty('id')) {
            return namespace_1.NamespaceId.createFromEncoded(recipientAddress.id);
        }
    }
    throw new Error(`Recipient: ${recipientAddress} type is not recognised`);
};
/**
 * Extract mosaics from encoded UInt64 notation.
 *
 * If most significant bit of byte 0 is set, then it is a namespaceId.
 * If most significant bit of byte 0 is not set, then it is a mosaicId.
 *
 * @param mosaics {Array | undefined} The DTO array of mosaics (with UInt64 Id notation)
 * @return {Mosaic[]}
 */
exports.extractMosaics = (mosaics) => {
    if (mosaics === undefined) {
        return [];
    }
    return mosaics.map((mosaicDTO) => {
        const id = utils_1.UnresolvedMapping.toUnresolvedMosaic(mosaicDTO.id);
        return new mosaic_1.Mosaic(id, model_1.UInt64.fromNumericString(mosaicDTO.amount));
    });
};
/**
 * Extract deadline from json payload.
 * @param deadline - deadline dto
 */
const extractDeadline = (deadline) => {
    if (!deadline) {
        return transaction_1.Deadline.createEmtpy();
    }
    return transaction_1.Deadline.createFromDTO(deadline);
};
/**
 * @internal
 * Extract transaction meta data
 *
 * @param meta - Transaction meta data
 * @param id - TransactionId
 * @return {TransactionInfo | AggregateTransactionInfo | undefined}
 */
const extractTransactionMeta = (meta, id) => {
    if (!meta) {
        return undefined;
    }
    if (meta.aggregateHash || meta.aggregateId) {
        return new transaction_1.AggregateTransactionInfo(model_1.UInt64.fromNumericString(meta.height), meta.index, id, meta.aggregateHash, meta.aggregateId);
    }
    return new transaction_1.TransactionInfo(model_1.UInt64.fromNumericString(meta.height), meta.index, id, meta.hash, meta.merkleComponentHash);
};
/**
 * @internal
 * @param transactionDTO
 * @param transactionInfo
 * @param isEmbedded
 * @returns {any}
 * @constructor
 */
const CreateStandaloneTransactionFromDTO = (transactionDTO, transactionInfo, isEmbedded) => {
    const type = transactionDTO.type;
    const version = transactionDTO.version;
    const signature = transaction_1.Transaction.resolveSignature(transactionDTO.signature, false);
    const maxFee = model_1.UInt64.fromNumericString(transactionDTO.maxFee || '0');
    const deadline = extractDeadline(transactionDTO.deadline);
    if (type === transaction_1.TransactionType.TRANSFER) {
        return new transaction_1.TransferTransaction(transactionDTO.network, version, deadline, maxFee, exports.extractRecipient(transactionDTO.recipientAddress), exports.extractMosaics(transactionDTO.mosaics), model_1.MessageFactory.createMessageFromHex(transactionDTO.message), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.NAMESPACE_REGISTRATION) {
        return new transaction_1.NamespaceRegistrationTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.registrationType, transactionDTO.name, namespace_1.NamespaceId.createFromEncoded(transactionDTO.id), transactionDTO.registrationType === 0 ? model_1.UInt64.fromNumericString(transactionDTO.duration) : undefined, transactionDTO.registrationType === 1 ? namespace_1.NamespaceId.createFromEncoded(transactionDTO.parentId) : undefined, signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.MOSAIC_DEFINITION) {
        return new transaction_1.MosaicDefinitionTransaction(transactionDTO.network, version, deadline, maxFee, mosaic_1.MosaicNonce.createFromNumber(transactionDTO.nonce), new mosaic_1.MosaicId(transactionDTO.id), new mosaic_1.MosaicFlags(transactionDTO.flags), transactionDTO.divisibility, model_1.UInt64.fromNumericString(transactionDTO.duration), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.MOSAIC_SUPPLY_CHANGE) {
        return new transaction_1.MosaicSupplyChangeTransaction(transactionDTO.network, version, deadline, maxFee, utils_1.UnresolvedMapping.toUnresolvedMosaic(transactionDTO.mosaicId), transactionDTO.action, model_1.UInt64.fromNumericString(transactionDTO.delta), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.MULTISIG_ACCOUNT_MODIFICATION) {
        return new transaction_1.MultisigAccountModificationTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.minApprovalDelta, transactionDTO.minRemovalDelta, transactionDTO.addressAdditions ? transactionDTO.addressAdditions.map((addition) => exports.extractRecipient(addition)) : [], transactionDTO.addressDeletions ? transactionDTO.addressDeletions.map((deletion) => exports.extractRecipient(deletion)) : [], signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.HASH_LOCK) {
        const networkType = transactionDTO.network;
        return new transaction_1.LockFundsTransaction(networkType, version, deadline, maxFee, new mosaic_1.Mosaic(new mosaic_1.MosaicId(transactionDTO.mosaicId), model_1.UInt64.fromNumericString(transactionDTO.amount)), model_1.UInt64.fromNumericString(transactionDTO.duration), new transaction_1.SignedTransaction('', transactionDTO.hash, '', transaction_1.TransactionType.AGGREGATE_BONDED, networkType), signature, transactionDTO.signerPublicKey ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, networkType) : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.SECRET_LOCK) {
        const recipientAddress = transactionDTO.recipientAddress;
        const mosaicId = utils_1.UnresolvedMapping.toUnresolvedMosaic(transactionDTO.mosaicId);
        return new transaction_1.SecretLockTransaction(transactionDTO.network, version, deadline, maxFee, new mosaic_1.Mosaic(mosaicId, model_1.UInt64.fromNumericString(transactionDTO.amount)), model_1.UInt64.fromNumericString(transactionDTO.duration), transactionDTO.hashAlgorithm, transactionDTO.secret, exports.extractRecipient(recipientAddress), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.SECRET_PROOF) {
        const recipientAddress = transactionDTO.recipientAddress;
        return new transaction_1.SecretProofTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.hashAlgorithm, transactionDTO.secret, exports.extractRecipient(recipientAddress), transactionDTO.proof, signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.MOSAIC_ALIAS) {
        return new transaction_1.MosaicAliasTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.aliasAction, namespace_1.NamespaceId.createFromEncoded(transactionDTO.namespaceId), new mosaic_1.MosaicId(transactionDTO.mosaicId), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.ADDRESS_ALIAS) {
        return new transaction_1.AddressAliasTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.aliasAction, namespace_1.NamespaceId.createFromEncoded(transactionDTO.namespaceId), exports.extractRecipient(transactionDTO.address), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.ACCOUNT_ADDRESS_RESTRICTION) {
        return new transaction_1.AccountAddressRestrictionTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.restrictionFlags, transactionDTO.restrictionAdditions ? transactionDTO.restrictionAdditions.map((addition) => exports.extractRecipient(addition)) : [], transactionDTO.restrictionDeletions ? transactionDTO.restrictionDeletions.map((deletion) => exports.extractRecipient(deletion)) : [], signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.ACCOUNT_OPERATION_RESTRICTION) {
        return new transaction_1.AccountOperationRestrictionTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.restrictionFlags, transactionDTO.restrictionAdditions ? transactionDTO.restrictionAdditions : [], transactionDTO.restrictionDeletions ? transactionDTO.restrictionDeletions : [], signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.ACCOUNT_MOSAIC_RESTRICTION) {
        return new transaction_1.AccountMosaicRestrictionTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.restrictionFlags, transactionDTO.restrictionAdditions
            ? transactionDTO.restrictionAdditions.map((addition) => utils_1.UnresolvedMapping.toUnresolvedMosaic(addition))
            : [], transactionDTO.restrictionDeletions
            ? transactionDTO.restrictionDeletions.map((deletion) => utils_1.UnresolvedMapping.toUnresolvedMosaic(deletion))
            : [], signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.ACCOUNT_KEY_LINK) {
        return new transaction_1.AccountKeyLinkTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.linkedPublicKey, transactionDTO.linkAction, signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.MOSAIC_GLOBAL_RESTRICTION) {
        return new transaction_1.MosaicGlobalRestrictionTransaction(transactionDTO.network, version, deadline, maxFee, utils_1.UnresolvedMapping.toUnresolvedMosaic(transactionDTO.mosaicId), utils_1.UnresolvedMapping.toUnresolvedMosaic(transactionDTO.referenceMosaicId), model_1.UInt64.fromHex(transactionDTO.restrictionKey), model_1.UInt64.fromNumericString(transactionDTO.previousRestrictionValue), transactionDTO.previousRestrictionType, model_1.UInt64.fromNumericString(transactionDTO.newRestrictionValue), transactionDTO.newRestrictionType, signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.MOSAIC_ADDRESS_RESTRICTION) {
        return new transaction_1.MosaicAddressRestrictionTransaction(transactionDTO.network, version, deadline, maxFee, utils_1.UnresolvedMapping.toUnresolvedMosaic(transactionDTO.mosaicId), model_1.UInt64.fromHex(transactionDTO.restrictionKey), exports.extractRecipient(transactionDTO.targetAddress), model_1.UInt64.fromNumericString(transactionDTO.previousRestrictionValue), model_1.UInt64.fromNumericString(transactionDTO.newRestrictionValue), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.ACCOUNT_METADATA) {
        return new transaction_1.AccountMetadataTransaction(transactionDTO.network, version, deadline, maxFee, exports.extractRecipient(transactionDTO.targetAddress), model_1.UInt64.fromHex(transactionDTO.scopedMetadataKey), transactionDTO.valueSizeDelta, format_1.Convert.decodeHex(transactionDTO.value), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.MOSAIC_METADATA) {
        return new transaction_1.MosaicMetadataTransaction(transactionDTO.network, version, deadline, maxFee, exports.extractRecipient(transactionDTO.targetAddress), model_1.UInt64.fromHex(transactionDTO.scopedMetadataKey), utils_1.UnresolvedMapping.toUnresolvedMosaic(transactionDTO.targetMosaicId), transactionDTO.valueSizeDelta, format_1.Convert.decodeHex(transactionDTO.value), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.NAMESPACE_METADATA) {
        return new transaction_1.NamespaceMetadataTransaction(transactionDTO.network, version, deadline, maxFee, exports.extractRecipient(transactionDTO.targetAddress), model_1.UInt64.fromHex(transactionDTO.scopedMetadataKey), namespace_1.NamespaceId.createFromEncoded(transactionDTO.targetNamespaceId), transactionDTO.valueSizeDelta, format_1.Convert.decodeHex(transactionDTO.value), signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.VRF_KEY_LINK) {
        return new transaction_1.VrfKeyLinkTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.linkedPublicKey, transactionDTO.linkAction, signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.NODE_KEY_LINK) {
        return new transaction_1.NodeKeyLinkTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.linkedPublicKey, transactionDTO.linkAction, signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    else if (type === transaction_1.TransactionType.VOTING_KEY_LINK && version == model_1.TransactionVersion.VOTING_KEY_LINK) {
        return new transaction_1.VotingKeyLinkTransaction(transactionDTO.network, version, deadline, maxFee, transactionDTO.linkedPublicKey, transactionDTO.startEpoch, transactionDTO.endEpoch, transactionDTO.linkAction, signature, transactionDTO.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.signerPublicKey, transactionDTO.network)
            : undefined, transactionInfo).setPayloadSize(transactionDTO.size);
    }
    throw new Error(`Unimplemented transaction with type ${type} for version ${version}`);
};
/**
 * @internal
 * @param transactionDTO
 * @returns {Transaction}
 * @constructor
 */
exports.CreateTransactionFromDTO = (transactionDTO) => {
    if (transactionDTO.transaction.type === transaction_1.TransactionType.AGGREGATE_COMPLETE ||
        transactionDTO.transaction.type === transaction_1.TransactionType.AGGREGATE_BONDED) {
        const innerTransactions = transactionDTO.transaction.transactions
            ? transactionDTO.transaction.transactions.map((innerTransactionDTO) => {
                const aggregateTransactionInfo = extractTransactionMeta(innerTransactionDTO.meta, innerTransactionDTO.id);
                innerTransactionDTO.transaction.maxFee = transactionDTO.transaction.maxFee;
                innerTransactionDTO.transaction.deadline = transactionDTO.transaction.deadline;
                innerTransactionDTO.transaction.signature = transactionDTO.transaction.signature;
                return CreateStandaloneTransactionFromDTO(innerTransactionDTO.transaction, aggregateTransactionInfo, true);
            })
            : [];
        return new transaction_1.AggregateTransaction(transactionDTO.transaction.network, transactionDTO.transaction.type, transactionDTO.transaction.version, extractDeadline(transactionDTO.transaction.deadline), model_1.UInt64.fromNumericString(transactionDTO.transaction.maxFee || '0'), innerTransactions, transactionDTO.transaction.cosignatures
            ? transactionDTO.transaction.cosignatures.map((aggregateCosignatureDTO) => {
                return new transaction_1.AggregateTransactionCosignature(aggregateCosignatureDTO.signature, account_1.PublicAccount.createFromPublicKey(aggregateCosignatureDTO.signerPublicKey, transactionDTO.transaction.network), model_1.UInt64.fromNumericString(aggregateCosignatureDTO.version));
            })
            : [], transaction_1.Transaction.resolveSignature(transactionDTO.transaction.signature, false), transactionDTO.transaction.signerPublicKey
            ? account_1.PublicAccount.createFromPublicKey(transactionDTO.transaction.signerPublicKey, transactionDTO.transaction.network)
            : undefined, extractTransactionMeta(transactionDTO.meta, transactionDTO.id)).setPayloadSize(transactionDTO.transaction.size);
    }
    else {
        return CreateStandaloneTransactionFromDTO(transactionDTO.transaction, extractTransactionMeta(transactionDTO.meta, transactionDTO.id), false);
    }
};
//# sourceMappingURL=CreateTransactionFromDTO.js.map